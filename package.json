{
  "name": "towles-tool-rs",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/client"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "client:dev": "npm run dev --workspace @towles-tool/client"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  }
}
