{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "Core permissions for the main window (event listen/emit, core APIs, notifications, opening external URLs)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "opener:default"
  ]
}
