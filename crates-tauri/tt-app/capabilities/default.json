{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "Core permissions for the main window (event listen/emit, core APIs)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
