{
  "description": "Nudge the towles-tool app to refresh PR data right after `gh pr merge`/`gh pr create`, instead of waiting for its normal poll interval.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/gh-pr-nudge.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
