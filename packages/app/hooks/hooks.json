{
  "description": "Nudge the towles-tool app to refresh its PR/issue data right after a `gh pr`/`gh issue` mutation, instead of waiting for its normal poll interval.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/gh-pr-nudge.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
