{
  "name": "towles-tool-app",
  "description": "Bridges Claude Code to the towles-tool desktop app: registers its MCP server (board tasks: task_list, task_status, and the gated task_create) and a hook that nudges the app to refresh PR/issue data immediately after a `gh pr`/`gh issue` mutation.",
  "version": "0.1.4",
  "author": {
    "name": "Chris Towles"
  }
}
