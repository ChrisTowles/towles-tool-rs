---
description: Write a short prompt to hand this work off to a fresh agent
argument-hint: [optional note]
---

Write a short prompt I can hand to a fresh agent to restart this: the goal in
one line, what's done, what's next. Nothing else — no process narrative, no
file dumps. A few lines, not a doc.

Then copy it to my clipboard (`wl-copy` on Linux, `pbcopy` on macOS — pick
whichever is on PATH) so I can paste it straight into a new session.

$ARGUMENTS
