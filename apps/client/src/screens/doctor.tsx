import { NotWired } from "@/components/not-wired";

export function DoctorScreen() {
  return (
    <NotWired
      title="Doctor"
      detail="Not wired yet. Build this against the `ttr doctor` command (tool versions, gh auth, plugins) so it shows real check results."
    />
  );
}
