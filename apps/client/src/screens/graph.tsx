import { NotWired } from "@/components/not-wired";

export function GraphScreen() {
  return (
    <NotWired
      title="Graph"
      detail="Not wired yet. Build this against the tt-graph session-JSONL accounting so it shows real Claude Code token usage per project."
    />
  );
}
