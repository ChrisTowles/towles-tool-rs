import { NotWired } from "@/components/not-wired";

export function JournalTodayScreen() {
  return (
    <NotWired
      title="Today"
      detail="Not wired yet. Build this against the `ttr journal` commands (tt-journal crate) so it shows and appends to the real daily note."
    />
  );
}
